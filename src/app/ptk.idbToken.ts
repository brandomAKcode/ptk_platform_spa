// class to connect to indexedDB and get token to verify access
export class PTKIDBToken {
    /* Class to connect to IndexedDB y get and put token from ptk platform */
    idb_name: string = 'ptk_idb';
    version: number = 1;
}